# 更新与发布指南

给未来的自己（或接手这个项目的人）：怎么加图标、改功能、发新版本。

> 所有命令都在项目文件夹里执行。
> 路径：`C:\Users\lenovo\WorkBuddy\2026-09-19-21-25-32\pet-cursor`

---

## 零、两种方式：用 bat 还是敲命令

| | 适合 | 怎么用 |
| --- | --- | --- |
| **bat 脚本**（推荐） | 不想管 Python 环境 | 双击 `加图标.bat` / `打包发布.bat` |
| **命令行** | 想精细控制 | 看下面各节的命令 |

**两个 bat 会自动装好缺的依赖**（走清华源，国内快），不用你手动配环境：

- **`加图标.bat`** —— 把图片**拖到它上面**，或双击后填路径，然后输入名字
- **`打包发布.bat`** —— 自动打包 exe + 生成发布 zip

> 本机系统 Python（3.13.14）**没装 Pillow 和 PyInstaller**，
> 所以直接敲 `python add_icon.py` 会报 `No module named 'PIL'`。
> 用上面的 bat，或先执行一次：
> ```bash
> pip install Pillow pyinstaller -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

---

## 一、先分清两种「版本号」（最容易搞混）

| 名字 | 在哪 | 控制什么 | 什么时候改 |
| --- | --- | --- | --- |
| **图标库版本** | `catalog.json` 里的 `version` | 别人客户端**要不要重新下载图标** | 只要动过 `icons/` 或 `catalog.json` |
| **软件版本** | Release 的 tag（v1.0.0） | 别人下载的 exe 是第几版 | 改了功能代码、要发新包时 |

**这两个互相独立。** 只加图标不改代码 → 只改 `version`，不用发新 Release；
改了 `server.py` / 前端 → 发新 Release，但没动图标就不用改 `version`。

---

## 二、场景 A：加新图标（最常用）

### 1. 加图

```bash
python add_icon.py 我的猫.png --name 小橘 --cat 动物 --emoji 🐱
```

它会自动：把图规整成 512×512 透明 PNG 放进 `icons/` → 往 `catalog.json` 加一条 → **把 `version` 改成今天日期**。

参数说明：

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| 第一个位置参数 | ✅ | 图片路径（png/jpg/webp 都行） |
| `--name` | ✅ | 图标名字，比如 `小橘` |
| `--cat` | | 分类，默认 `我的图标`；建议用现有的 `昆虫` / `动物` / `奇幻` |
| `--emoji` | | 可选的 emoji 标记 |
| `--no-bump` | | **不改 version**（一次加很多张时用它，全加完再手动改一次） |

> 文件名会自动转成 ASCII。中文 id 进 jsDelivr 的 URL 要百分号编码，容易出问题。

### 2. 批量加

```bash
python add_icon.py a.png --name 阿波罗 --cat 奇幻 --no-bump
python add_icon.py b.png --name 小熊猫 --cat 动物 --no-bump
python add_icon.py c.png --name 蜜蜂   --cat 昆虫 --no-bump
```

然后手动改 `catalog.json` 第一行的 `version`（比如 `2026.09.21` → `2026.10.03`）。

> ⚠️ **这一步千万别忘**。不改的话，别人的客户端认为"已是最新"，根本不会下载你的新图标。

### 3. 推送

```bash
git add icons/ catalog.json
git commit -m "新增图标：小橘、阿波罗、小熊猫"
git push
```

推完 **几分钟内** jsDelivr 生效，所有人的软件下次启动自动拉到新图标（不用发 Release）。

---

## 三、场景 B：改了功能代码

改完 `server.py` / `web/index.html` / `privacy.py` 等之后：

```bash
git add -A
git commit -m "说清楚改了什么"
git push
```

**改完要自己测一遍**再推：

```bash
python server.py
```
浏览器开 http://127.0.0.1:8899 点一遍相关功能。

> 注意：改的是**源码**，别人从 GitHub 拉源码后要自己跑；
> 想让下载 exe 的人也用上新功能，必须走下面的场景 C。

---

## 四、场景 C：发新版本（让别人下载到新 exe）

### 1. 决定版本号

| 改动类型 | 版本号怎么跳 | 例子 |
| --- | --- | --- |
| 修 bug、小调整 | 最后一位 +1 | v1.0.0 → v1.0.1 |
| 加了新功能 | 中间一位 +1 | v1.0.0 → v1.1.0 |
| 大改版 / 不兼容 | 第一位 +1 | v1.0.0 → v2.0.0 |

### 2. 打包并生成 zip

```bash
pyinstaller 小虫光标.spec
python make_release.py
```

产物：`release_pkg/PetCursor-Windows.zip`

`make_release.py` 会自动剔除私人数据（`user_icons/`、下载计数），
并在打包后自检——发现残留会直接中止，不会生成有问题的包。

> ⚠️ exe 跑过会在自己旁边建一个空的 `user_icons/` 目录。
> 所以**每次打包前最好先删掉 `dist/`**，别让上次运行的残留混进去。

### 3. 提交源码

```bash
git add -A
git commit -m "v1.1.0：加了 XXX 功能"
git push
```

### 4. 发 Release

1. 打开 https://github.com/llpp666/pet-cursor/releases
2. 点 **Draft a new release**
3. **Choose a tag** → 填 `v1.1.0` → 选 **Create new tag**
4. Release title：`小虫光标 v1.1.0`
5. 描述里写清楚这版改了什么
6. **把 `release_pkg/PetCursor-Windows.zip` 拖进下面的上传区**
7. 点 **Publish release**

> ⚠️ **文件名必须是英文**。实测上传 `小虫光标-Windows.zip`，
> GitHub 会把中文吃掉变成 `-Windows.zip`。zip 里面的文件夹叫中文没关系。

---

## 五、推送不上怎么办

### GitHub 连不上（Connection reset / timeout）

这台机器的网络直连 GitHub 会被掐，必须走 Clash 代理 **`127.0.0.1:7897`**：

```bash
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main
```

嫌每次打太长，可以设一次全局代理（**Clash 关掉时要记得取消**，否则 git 会一直超时）：

```bash
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897

# 取消：
git config --global --unset http.proxy
git config --global --unset https.proxy
```

### 要密码 / 认证失败

GitHub **不接受登录密码**，要用 Personal Access Token（PAT）：

1. GitHub 头像 → **Settings** → **Developer settings**
   → **Personal access tokens** → **Tokens (classic)**
   → **Generate new token (classic)**
2. 勾 **repo**，有效期选 7 天或 90 天
3. 生成后复制那串 `ghp_` 开头的字符
4. 弹密码框时**粘贴它**

**用完立刻回去 Delete 掉这个 token**，别留着。

---

## 六、几个已经踩过的坑（别再踩）

| 坑 | 后果 | 怎么避免 |
| --- | --- | --- |
| 改了图标但没改 `version` | 别人永远拉不到新图标 | 动过 `icons/` 就改 `version` |
| Release 用中文文件名 | 名字变 `-Windows.zip` | 一律用 `PetCursor-Windows.zip` |
| 打包前没删 `dist/` | 私人图片可能混进发布包 | `make_release.py` 会拦，但仍建议先删 |
| 在代码里写死绝对路径 | 别人机器上跑不起来 | 路径用相对路径或 `os.environ` 拼 |
| 忘了删 token | 账号有风险 | 推完立刻 Revoke |

---

## 七、不想敲命令：让 AI 帮你做

直接跟 AI 说：

> 「我加了三个图标，帮我推上去」
>
> 「改了 server.py 的 XX，打包发个 v1.1.0」

需要推送时 AI 会跟你要一次 token（生成方法见第五节），
推完记得自己去 GitHub 删掉。

---

## 八、常用命令速查

```bash
# ── 懒人方式（推荐）────────────────
加图标.bat          # 双击，或把图片拖上去
打包发布.bat        # 双击，自动出 zip

# ── 加一个图标 ────────────────────
python add_icon.py 图片.png --name 名字 --cat 分类

# ── 启动软件（源码方式，用于测试）──
python server.py

# 打包 exe
pyinstaller 小虫光标.spec

# 生成发布 zip
python make_release.py

# 看看改了什么
git status
git diff

# 提交 + 推送
git add -A
git commit -m "说明"
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main

# 看看提交历史
git log --oneline -5
```

---

## 九、发布后别人什么时候能拿到

| 渠道 | 生效时间 |
| --- | --- |
| 图标更新（只改 `catalog.json`） | jsDelivr 缓存几分钟，别人下次启动软件自动拉 |
| exe 新版本（发 Release） | 立刻，但要别人**主动去下载** |

软件**不会自动升级 exe** —— 没有服务器，做不到自动更新。
想让下载 exe 的人升级，只能在 Release 说明里写清楚，让他们重新下载。
